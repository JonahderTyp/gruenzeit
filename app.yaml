runtime: python39

instance_class: F2

entrypoint: gunicorn -b :$PORT run:app

# env_variables:
#   SECRET_KEY: 'hi0eS0TMVZwie82mpwVDJw'

beta_settings:
  cloud_sql_instances: gruenzeit:europe-west1:gzdb1

handlers:
- url: /static
  static_dir: gruenzeit/static

- url: /.*
  script: auto
